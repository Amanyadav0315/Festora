PORT=4000
MONGO_URI=mongodb+srv://vipinbhati418_db_user:<DB_PASSWORD>@eventsamancluster.vvntsg7.mongodb.net/eventsaman?appName=EventSamanCluster
JWT_ACCESS_SECRET=<generate a long random secret — do NOT reuse the dev one>
JWT_REFRESH_SECRET=<generate a different long random secret — do NOT reuse the dev one>
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
CORS_ORIGIN=https://eventsaman.com,https://admin.eventsaman.com
